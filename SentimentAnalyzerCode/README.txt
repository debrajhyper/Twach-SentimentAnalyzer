Welcome to our Twitter Sentiment Analysis web application.

To run this web app locally follow these steps:

1> Open the python file "app.py".
2> Open the terminal window in the same directory as app.py.
3> Execute the command "streamlit run app.py" in the terminal window.

The web application should open in your default browser.


Thankyou for using our app.